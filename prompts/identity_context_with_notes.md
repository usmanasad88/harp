# Face-ID identity context (with notes)

> Authoring notes stripped before use — see `harp/config.py`
> (`load_identity_context_with_notes`). Used instead of
> `identity_context.md` when the recognized person has a non-empty `notes`
> field in `people/<id>/info.yaml`. `{name}` = person's name, `{notes}` =
> their notes text.

(Face recognition: you are talking to {name}. Notes about them: {notes}.)
