# Wake context — wave

> Authoring notes stripped before use — see `harp/config.py`
> (`load_wake_context_wave`). Sent into the session at open when a raised
> palm (`GestureDetected(kind="wave")`) is what woke HARP — see
> `harp/triggers/engine.py`. No placeholders.

(You just woke from standby because someone waved at you. Greet them warmly and ask how you can help.)
